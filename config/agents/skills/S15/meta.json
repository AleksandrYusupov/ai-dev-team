{
  "id": "S15",
  "version": "v1",
  "name": "Frontend Implementation Pack",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Правила UI-компонентов, accessibility, state management, routing, forms, loading/error states, design system adherence, analytics hooks.",
    "Seed rationale: Для FrontendBuildAgent."
  ],
  "inputs": [
    "Включить responsive, a11y, skeleton/error/empty states, component tests, visual consistency."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Правила UI-компонентов, accessibility, state management, routing, forms, loading/error states, design system adherence, analytics hooks.",
  "why": "Для FrontendBuildAgent.",
  "download_ref": null,
  "build_spec": "Включить responsive, a11y, skeleton/error/empty states, component tests, visual consistency.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S15",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
