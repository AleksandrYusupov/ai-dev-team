{
  "id": "S28",
  "version": "v1",
  "name": "Changelog & Release Notes Generator",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Генерирует change summaries из commits/PRs/issues по аудиториям: engineers, stakeholders, customers.",
    "Seed rationale: Нужен ReleaseAgent и DocsAgent."
  ],
  "inputs": [
    "Разделять internal-only vs customer-facing, breaking changes, migration notes."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Генерирует change summaries из commits/PRs/issues по аудиториям: engineers, stakeholders, customers.",
  "why": "Нужен ReleaseAgent и DocsAgent.",
  "download_ref": null,
  "build_spec": "Разделять internal-only vs customer-facing, breaking changes, migration notes.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S28",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
