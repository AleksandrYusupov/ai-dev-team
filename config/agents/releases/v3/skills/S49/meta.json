{
  "id": "S49",
  "version": "v1",
  "name": "Provider Capability & Runner Network Fit Validator",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [
    "vendor-docs-mcp",
    "secret-broker-mcp",
    "oauth-broker-mcp",
    "integration-lab-mcp"
  ],
  "sensitivity_class": "credential_boundary",
  "when_to_use": [
    "Работает с metadata-only Secrets/Auth plane: credential_slots, oauth_client_registrations, oauth_consent_sessions, token_handles, webhook_registrations, integration_validation_runs.",
    "Seed rationale: Нужен, чтобы orchestration layer видел readiness/auth state, но не становился секрет-хранилищем."
  ],
  "inputs": [
    "Должен поддерживать create/read/update flow для metadata-only сущностей, lookup secret aliases/handles, revoke/rotation state, client registration facts и validation run history. Raw secret material, auth codes и bearer tokens сохранять запрещено."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks.",
    "Do not request, paste, persist, or summarize raw secrets, tokens, browser session dumps, or vendor-console exports.",
    "Do not move credential truth into prompt bundles, context packs, repo docs, or Linear comments.",
    "Do not collapse the metadata plane and credential plane into one artifact or one instruction surface."
  ],
  "human_gate": {
    "required": true,
    "zones": [
      "credential_ownership_vendor_console_actions",
      "protected_deploy"
    ],
    "notes": "Integration-sensitive skill: human approval remains mandatory around consent, credentials, and go-live."
  },
  "description": "Validates that the runner host and network environment are compatible with the target provider: outbound connectivity, IP allowlisting, required SDK/runtime availability, sandbox environment existence, and network policy alignment.",
  "why": "An integration can have perfect auth and clean code but still fail at runtime if the runner cannot reach the provider or lacks the required runtime. This skill catches environment mismatches before code is written.",
  "download_ref": null,
  "build_spec": "Должен поддерживать create/read/update flow для metadata-only сущностей, lookup secret aliases/handles, revoke/rotation state, client registration facts и validation run history. Raw secret material, auth codes и bearer tokens сохранять запрещено.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S49",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
