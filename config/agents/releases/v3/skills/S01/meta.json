{
  "id": "S01",
  "version": "v1",
  "name": "Triage Classifier",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Классифицирует issue по type/risk/source/mode/area и определяет, нужен ли spec, input или можно идти дальше.",
    "Seed rationale: Основа IntakeAgent."
  ],
  "inputs": [
    "Модель multilabel classification + confidence + rationale + required next status."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Классифицирует issue по type/risk/source/mode/area и определяет, нужен ли spec, input или можно идти дальше.",
  "why": "Основа IntakeAgent.",
  "download_ref": null,
  "build_spec": "Модель multilabel classification + confidence + rationale + required next status.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S01",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
