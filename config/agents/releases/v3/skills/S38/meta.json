{
  "id": "S38",
  "version": "v1",
  "name": "Repository Registry Sync",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Синхронизирует backend registry ↔ Linear projects/labels/links ↔ repo metadata.",
    "Seed rationale: Критично для маршрутизации и источника истины."
  ],
  "inputs": [
    "Bi-directional but registry is canonical. Detect drift and propose fixes."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Синхронизирует backend registry ↔ Linear projects/labels/links ↔ repo metadata.",
  "why": "Критично для маршрутизации и источника истины.",
  "download_ref": null,
  "build_spec": "Bi-directional but registry is canonical. Detect drift and propose fixes.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S38",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
