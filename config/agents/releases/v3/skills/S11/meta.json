{
  "id": "S11",
  "version": "v1",
  "name": "Migration & Data Change Planner",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Планирует schema/data migrations, backward compatibility, rollout gates, rollback windows.",
    "Seed rationale: Критично для high-risk work."
  ],
  "inputs": [
    "Разделять expand/migrate/contract шаги, backfill, verification, rollback safety."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Планирует schema/data migrations, backward compatibility, rollout gates, rollback windows.",
  "why": "Критично для high-risk work.",
  "download_ref": null,
  "build_spec": "Разделять expand/migrate/contract шаги, backfill, verification, rollback safety.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S11",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
