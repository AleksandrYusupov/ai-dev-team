{
  "id": "S27",
  "version": "v1",
  "name": "Docs Synchronizer",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Обновляет README, runbooks, ADR index, module docs, PR summary, diagrams и release notes при изменении кода.",
    "Seed rationale: OpenAI рекомендует встраивать docs прямо в delivery pipeline."
  ],
  "inputs": [
    "Detect stale docs from diff; propose exact files to update; generate mermaid when useful. Для integration work обновлять webhook contracts, runbooks, go-live checklists и rollout notes без утечки raw-secret/auth truth."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Обновляет README, runbooks, ADR index, module docs, PR summary, diagrams и release notes при изменении кода.",
  "why": "OpenAI рекомендует встраивать docs прямо в delivery pipeline.",
  "download_ref": null,
  "build_spec": "Detect stale docs from diff; propose exact files to update; generate mermaid when useful. Для integration work обновлять webhook contracts, runbooks, go-live checklists и rollout notes без утечки raw-secret/auth truth.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S27",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
