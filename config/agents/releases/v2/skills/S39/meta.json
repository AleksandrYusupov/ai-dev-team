{
  "id": "S39",
  "version": "v1",
  "name": "Dependency Update Executor",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "human_gate_required",
  "when_to_use": [
    "Автоматизирует safe dependency refresh с changelog summarization, targeted tests, rollout note и rollback plan.",
    "Seed rationale: Для DependencyAgent."
  ],
  "inputs": [
    "Batching policy, critical CVE fast lane, low-risk grouped updates."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks.",
    "Do not execute protected, irreversible, or approval-bound actions without the declared human gate."
  ],
  "human_gate": {
    "required": true,
    "zones": [
      "architecture_sign_off",
      "final_review_merge",
      "protected_deploy"
    ],
    "notes": "Risk-sensitive skill: final execution must stay behind explicit human approval."
  },
  "description": "Автоматизирует safe dependency refresh с changelog summarization, targeted tests, rollout note и rollback plan.",
  "why": "Для DependencyAgent.",
  "download_ref": null,
  "build_spec": "Batching policy, critical CVE fast lane, low-risk grouped updates.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S39",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
