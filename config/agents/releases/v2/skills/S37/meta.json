{
  "id": "S37",
  "version": "v1",
  "name": "CI/CD Workflow Bootstrap",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Поднимает build/test/review/deploy pipelines и required checks по golden path.",
    "Seed rationale: Для Platform/Provisioner."
  ],
  "inputs": [
    "Нужно на уровне template repo + validations + secrets policy."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Поднимает build/test/review/deploy pipelines и required checks по golden path.",
  "why": "Для Platform/Provisioner.",
  "download_ref": null,
  "build_spec": "Нужно на уровне template repo + validations + secrets policy.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S37",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
