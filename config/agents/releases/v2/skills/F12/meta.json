{
  "id": "F12",
  "version": "v1",
  "name": "Evaluation & Benchmark Harness",
  "category": "foundation",
  "availability": "custom",
  "kind": "foundation",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Запускает eval-наборы для агентов/скиллов, собирает quality metrics, variance, false-positive/false-negative patterns и regression alerts.",
    "Seed rationale: Без evals агентная команда деградирует незаметно."
  ],
  "inputs": [
    "Нужно хранить gold tasks и gold PRs, поддерживать before/after benchmark по skill versions и правила автоотката неудачных изменений."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Запускает eval-наборы для агентов/скиллов, собирает quality metrics, variance, false-positive/false-negative patterns и regression alerts.",
  "why": "Без evals агентная команда деградирует незаметно.",
  "download_ref": {
    "title": "Anthropic skill-creator",
    "url": "https://github.com/anthropics/claude-plugins-official/blob/main/plugins/skill-creator/skills/skill-creator/SKILL.md"
  },
  "build_spec": "Нужно хранить gold tasks и gold PRs, поддерживать before/after benchmark по skill versions и правила автоотката неудачных изменений.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/F12",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
