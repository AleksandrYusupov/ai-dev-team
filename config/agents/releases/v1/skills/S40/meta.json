{
  "id": "S40",
  "version": "v1",
  "name": "Tech Debt & Stale Code Detector",
  "category": "custom",
  "availability": "custom",
  "kind": "custom",
  "runtime_dependency": true,
  "reference_only_default": false,
  "provider_compatibility": [
    "codex",
    "claude"
  ],
  "required_tools": [],
  "required_mcp": [],
  "sensitivity_class": "standard",
  "when_to_use": [
    "Находит feature flags to remove, dead code, stale docs, deprecated APIs, low-value toil automation targets.",
    "Seed rationale: Даёт отделу не только доставку, но и постоянное оздоровление системы."
  ],
  "inputs": [
    "Регулярные scans + issue creation suggestions ranked by impact."
  ],
  "steps": [
    "TBD: convert the seed build spec into a stable step-by-step skill contract."
  ],
  "stop_conditions": [
    "TBD: define success and stop boundaries before runtime consumption."
  ],
  "escalation_rules": [
    "Escalate when the required source-of-truth inputs are missing, contradictory, or blocked by a human-owned zone."
  ],
  "anti_patterns": [
    "Do not duplicate always-on repo guidance inside selective skills.",
    "Do not silently widen role scope beyond the owning skill pack."
  ],
  "denied_actions": [
    "Do not treat this skeleton metadata as runtime-ready execution logic before the later runtime-consumption blocks."
  ],
  "human_gate": {
    "required": false,
    "zones": [],
    "notes": "TBD"
  },
  "description": "Находит feature flags to remove, dead code, stale docs, deprecated APIs, low-value toil automation targets.",
  "why": "Даёт отделу не только доставку, но и постоянное оздоровление системы.",
  "download_ref": null,
  "build_spec": "Регулярные scans + issue creation suggestions ranked by impact.",
  "source_refs": [
    "building_agents/ai_dept_agents_and_skills_manifest_v2.json#skills/S40",
    "source/Маршруты в зависимости от разных задач.md#11"
  ]
}
