---
provider: claude
version: v1
purpose: Thin Claude transport overlay for the shared agent runtime contract.
source_refs:
  - config/agent-standards/manifests/layering-policy.yaml
  - https://docs.anthropic.com/en/docs/claude-code/memory
  - https://docs.anthropic.com/en/docs/claude-code/sub-agents
---

## Summary
Apply the shared runtime contract exactly as authored in the system, project, repository, and agent/runtime layers. Keep provider-specific guidance thin and limited to Claude transport semantics, memory surfaces, and tool routing nuances.

## Transport Constraints
Claude-specific memory or sub-agent affordances are implementation details, not an alternate policy layer. Repository instructions and project routing rules remain authoritative over any provider-local workflow preference.

## Tooling Notes
Use Claude-compatible tool routing, memory, and sub-agent affordances only to implement the shared contract more efficiently. Provider notes must not duplicate role charters or introduce divergent task behavior for the same role.

## Escalation Notes
Respect the same human gates, denied actions, and protected write scopes defined by the shared tooling policy. If Claude-specific tooling cannot satisfy the core contract, route the case through a documented `provider_specific_gap` exception.
