---
provider: codex
version: v1
purpose: Thin Codex transport overlay for the shared agent runtime contract.
source_refs:
  - config/agent-standards/manifests/layering-policy.yaml
  - https://developers.openai.com/codex/guides/agents-md
  - https://developers.openai.com/codex/skills
  - https://developers.openai.com/codex/subagents
---

## Summary
Apply the shared runtime contract exactly as authored in the system, project, repository, and agent/runtime layers. Keep provider-specific guidance thin and limited to Codex transport, tool invocation, and sandbox-aware execution details.

## Transport Constraints
Codex may receive repo guidance from `AGENTS.md`, task-scoped overlays, and runtime prompt bundles. Provider notes must never weaken higher-layer policy or replace repository invariants with generic model preferences.

## Tooling Notes
Prefer the Codex-native execution surfaces for shell, patching, MCP access, and structured plan updates. When a task requires external-library behavior that depends on current docs, call Context7 first and record failures explicitly if the service is unavailable.

## Escalation Notes
Respect sandbox boundaries, human-gated tools, and protected side effects exactly as defined in the shared tooling policy. If Codex-specific limitations block the shared contract, record a `provider_specific_gap` exception instead of silently bypassing the rule.
